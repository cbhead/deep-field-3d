# Map validation ratchet — see docs/MAP-AUTHORING.md §4 and tools/map-validate.sh
#
# The number is how many §4 violations that map is currently allowed. CI fails
# if a map exceeds its baseline, so no map can get worse; when a map improves,
# lower the number in the same commit so the gain is locked in.
#
# These are not targets. Every one of them should end at 0.
#
# map	max	what is still wrong
foundry	0	clean — every rule the probe can measure holds
switchyard	0	clean — every rule the probe can measure holds
spire	84	never designed — 26 sockets with no way up, 32 of 43 deck pads with nothing under them, the air lane unreachable at most of its length. See MAP-AUTHORING.md §7. (Was 90; the drop is the probe no longer double-counting shared waypoints, not the map improving.)
toaster	0	clean on the day it landed — the first map generated against the rules rather than measured after the fact
