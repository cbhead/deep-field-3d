# INT digest — 2026-09-26 (the trunk moves to `main`)

Written by the cloud session that carried out ADR-0029 at the owner's request, acting INT for this
change only. It is not a full cycle: nothing was built, no lease was swept and no PR was reviewed. It
records one interface change that every session consumes, and lists what landed on the trunk since
cycle 3 for the next cycle to fold in. A full cycle run later on 2026-09-26 adds its own section
below this one rather than editing it.

## Interface change: the trunk is `main` (ADR-0029)
`unreal/main` was merged into `main` with a merge commit, so all 208 of its commits, and every
ledger-cited sha that was on it, are on `main`. `unreal/main` is **frozen at `d6a1ce0`**, the WS-15
prep. That commit and the WS-07 prep (`b577aab`) were pushed to `unreal/main` 53 and 13 minutes after
PR #62, while this change was being written, and both were merged in. The freeze is not locked yet
(see "For the human"). `game/` is still deleted at G3; only the branch move was brought forward.

The merge added no C++. `unreal/DeepField/` and `unreal/content/` on `main` are the frozen tip's, and
this change edits only plan documents, runbooks and `unreal/Build` tooling. So the trunk's build state
is exactly what it was: unmeasured since `fcc1e1d` (NEXT.md).

**This change lands as a merge commit** (the PR's *Create a merge commit*) or as a fast-forward push:
never a squash or a rebase, which would cut the 208 commits off `main` (ADR-0029 §1).

What every session does, once:
- **Switch the clone:** `git fetch origin`, `git switch main`, `git merge --ff-only origin/main`.
  Anything still on `unreal/main` builds a frozen tree, and a session that pushes a claim there
  strands it.
- **Retarget branches in flight:** merge or rebase onto `origin/main`, as you did onto
  `origin/unreal/main`. Only your own commits move, because `main` contains all of `unreal/main`.
- **PRs, claims and landings go to `main`:** PROGRAMME.md §6.2, §6.3 and §6.5, and `PLAN/README.md`
  ("Claiming in one command"), now say `main` / `origin/main`.

What moved with it: `unreal-checks` runs on pushes to `main` (its PR trigger was already
branch-agnostic); `unreal-win` builds `main`, which is also the branch it has to live on for its
nightly and its Run workflow button; `deepfield setup` clones `main`, and warns (never switches) when
it finds a clone on `unreal/main`; `ci.yml`'s frozen Godot/sim lanes take the same path filter on
`push` as on `pull_request`, so an Unreal landing does not run them. The Mac-era zsh scripts that no
longer run are retargeted too, so WS-15's `int-merge` port starts from the right trunk.

## Branches
The full table, and the sessions holding leases when the trunk moved, are in `NEXT.md` ("Branches").
In short:
- `main`: the trunk.
- `unreal/main`: frozen at `d6a1ce0`, fully merged, not yet locked. It is kept so old clones and links
  find a stale branch rather than a missing one.
- `claude/happy-babbage-t6qrhw`: **WS-04, in flight and not built.** Ten `[WS-04]` commits (the
  tower actor half, the build service, structures, the C9 rig, traps) that exist nowhere else. Its
  owner, or the next claimant once the lease lapses, merges `origin/main` and opens the PR against
  `main`.
- `ws/05-enemies-ai/waveplan`: fully landed. It can be deleted once WS-05's frontmatter stops naming it.
- **Correction:** NEXT.md listed PR #44 (WS-12) as stranded. It landed on 2026-09-25 as `411bb0b`.

## PRs merged on the trunk since cycle 3 (2026-09-24), and the two briefs pushed to it
Titles are shortened; each sha is the merge commit. None has a build or test result from the GPU box.
- `50dc5af` #48 — INT rulings R1–R14, the WS-28 match-flow shells, WS-09/WS-15 items (unbuilt; broke
  the build until `fcc1e1d`)
- `930b059` #50 — the `unreal/README.md` runbook
- `910e51b` #51 — `[WS-04]` tower logic core (unbuilt)
- `e99b517` #52, `48d3831` #53, `868ab65` #54, `e3f87b2` #55 — `deepfield.ps1`: one script that readies
  a Windows machine, then builds, tests and runs
- `411bb0b` #44 — `[WS-12]` UI tokens and the CommonUI screen/layer stack, with INT's three findings
  fixed
- `c7c8389` #49 — `[WS-05]` `UDFEnemyMovement`: walker state becomes a body
- `605ffb9` #56 — `[WS-03]` `ADFHeroCharacter` on a predicted hero CMC (unbuilt)
- `42a1cef` #57 — `[WS-19]` boss logic core as pure functions (unbuilt)
- `79e7caf` #58 — `[WS-03]` `UDFHeroStateComponent`: downed, bleedout, revive, seat (unbuilt)
- `b8e8c28` #59 — `[WS-03]` the hero's health: ASC on the hero, downs, revive/respawn heal (unbuilt)
- `33d4c6a` #60 — `[WS-28]` `ADFPlayerState` hosts the hero state (unbuilt)
- `486c46a` #62 — `[WS-10a]` Foundry on terrain: `level.schema.json`, the landform, the first
  authored level file
- `b577aab` (direct push) — WS-07 Factions prepped: an audited brief with twelve rulings, and three
  defects it found outside the workstream (WS-11, WS-03, C2)
- `d6a1ce0` (direct push) — WS-15 prepped: the `int-merge` port brief, with every guard it must keep

The other 19 direct pushes since cycle 3 (claims, renewals, INT ledger updates, and Build and CI
commits) are not listed: `git log --first-parent --no-merges 5894d92..d6a1ce0`.

`main` also brought two PRs of its own to the merge: `c8a6881` #39 (arrow keys in the frozen Godot
client, 2026-09-19) and `e2c5009` #61 (`[WS-15]` `unreal-win.yml` on `main`, now simply part of the
trunk).

## Red tests / unverified lanes
Unchanged by this move. Zero runners are registered, `unreal-win` reports *skipped*, and a green
`unreal-checks` means only that the ledger and schemas are consistent.

## For the human
1. **Lock `unreal/main` as soon as this lands**: a ruleset targeting `refs/heads/unreal/main` with
   *Restrict updates* (or a branch rule with *Lock branch*). The existing "Protect Trunks" ruleset
   blocks only deletion and force-push, and two briefs reached the branch while this change was being
   written. Make it a separate ruleset: "Protect Trunks" also covers `main`, which has to stay open to
   direct pushes because ledger claims land there (§6.2). Delete `unreal/main` only once no clone uses
   it.
2. **Tell every running session that the trunk moved.** The sessions holding leases are listed in
   NEXT.md. A session working from an old checkout follows its own copy of `PLAN/README.md` and pushes
   claims to `unreal/main`. Until the lock is on, INT merges anything that lands there (ADR-0029 §3).
3. **Switch the GPU box's clone to `main`** (`git fetch origin`, `git switch main`, `git merge
   --ff-only origin/main`), then re-run `machine-inventory.ps1`, so that `Build/machines/windows-gpu.*`
   stops recording `unreal/main`.
4. `ws/05-enemies-ai/waveplan` is fully on `main`. Delete it once WS-05's `branch:` moves off it.
