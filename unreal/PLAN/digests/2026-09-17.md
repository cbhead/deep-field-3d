# INT digest — 2026-09-17 (cycle 0: programme start)

**Branch:** `unreal/main` created from `main@71f6059` and pushed.

## Landed
- `f295266` — WS-00 P0 scaffold: PROGRAMME.md (plan of record), DECISIONS.md (ADR-0001..0018), OWNERSHIP.md, EDITOR-SLOTS.md, CONTRACTS/ (C1–C16 + map-authoring-3d), registry.json, 49 workstream files, `unreal/Build/plan-{status,scaffold,claim}.py`, LFS rules (`unreal/.gitattributes`, `.lfsconfig`).
- `b44eea1` — WS-01 claimed by the integration session.
- (this cycle) — WS-01 content bootstrap: `tools/content-export`, `unreal/content/{json,schema,levels/legacy,content-ids.json}`. The JSON is now the source of truth for content numbers (ADR-0005).

## Interface changes
- None (contracts frozen at their initial revision; no RFCs open).

## Workstream state
- WS-00 active (INT session) — remaining: the `.uproject`, modules, native tags, message structs, `L_Dev_Empty` — **blocked on the external SSD** (16 GB free; ADR-0016).
- WS-01 active (INT session) — JSON half done; commandlet half blocked on the `.uproject`.
- All other workstreams unclaimed. Claimable now without the SSD (code-only, `-nullrhi` or no engine at all): none until WS-00's `.uproject` exists — every C++ workstream compiles against it. What *is* claimable now: WS-15 (ledger CI checks: ownership, layering, schema validation, STATUS regeneration — pure Python), WS-30's terrain lane design (`build_heightmap.py` prototype against `terrain.json` — pure Python), WS-09's `map-authoring-3d` validator design notes, WS-10a's Foundry `terrain.json` + redesigned `level.json` authoring (text; validated once the importer exists).

## Blockers for the human (P0)
1. External NVMe SSD attached and the project path moved there (≥150 GB free at the path).
2. `brew install git-lfs && git lfs install` (this clone), GitHub LFS data pack.
3. EOS Developer Portal: product, Dev/Stage/Live sandboxes, client policies (game client never trusted-server); EGS onboarding + Epic Account Services brand review started.
4. GPU workstation ordered (Windows, RTX 4070+/64 GB/2 TB) — render, perf and Windows lanes stay "unverified" until it exists.

## Red tests / unverified lanes
- No automation exists yet. All render/perf lanes unverified (no GPU box).

## Editor slots
- Both free.
