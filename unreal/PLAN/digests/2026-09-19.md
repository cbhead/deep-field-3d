# INT digest — 2026-09-19 (cycle 1: the project exists)

**P0 unblocked:** external SSD attached (APFS, 3.6 TB) → the Unreal working copy is the SSD clone (ADR-0021); `git-lfs` installed and initialised in both checkouts; LFS upload to GitHub verified.

## Landed on `unreal/main`
- `181ea7c` — [WS-00] project skeleton: `.uproject` (UE 5.8.2, 26 plugins), targets (V7), 14 modules from `modules.json`, DFCore contracts (C1 tags, C15 messages + bus, C2 rows, content subsystem/definition), DFMatch game mode, C16 collision, render/nav/net/DDC config, `unreal/Build` scripts.
- `ff379ca` — ADR-0019 (message bus is ours), 0020 (installed-build constraints), 0021 (SSD working copy); README working-copies section.
- `e67929b` — [WS-00] `smoke-listen.sh` passes (listen host + headless client), CommonUI viewport client, quiet table loading before import.
- `4220fa2` — [WS-00] `L_Dev_Empty.umap` via LFS, `editor-lock.sh`, importer module deps pre-seeded (DFEditor: Landscape/LandscapeEditor/Foliage/AssetTools; DFCore exposes Json).
- `3cd3e3c` — ledger: WS-01/02/09/11/15/30 claimed for the agent batch.

## Interface changes
- None to frozen contracts. Contract-appends expected from the batch: `DFTintLayout.h` (CPD indices, WS-02), `Testing/DFTestUtils.h` in DFCore (WS-15), a `DFContentPipeline` editor module (WS-01; needs `modules.json` sync by INT).

## Workstream state
- WS-00 review (DoD met except "CI builds", which is WS-15's).
- WS-01 active: JSON half done; commandlet half in the batch.
- WS-02, WS-09, WS-11, WS-15, WS-30 active in the batch (branches `ws/NN-<slug>/<topic>`, worktrees `/Volumes/Toshiba/Deepfield-Unreal/wt-wsNN`). Merge order when they land: 15 (harness) → 01 (tables) → 02 → 09 → 30 → 11, each rebased and re-smoked.

## Blockers for the human
1. **EOS Developer Portal** product + sandboxes + client policies; **EGS onboarding** and Epic Account Services brand review — WS-11's spike stops at the Null services until credentials exist.
2. **GPU workstation** (Windows) — render/perf/Windows-packaging lanes remain unverified.
3. Optional: register this Mac as the nightly self-hosted runner once WS-15's workflow lands (`CONTRACTS/ci.md` will say how).

## Red tests / unverified lanes
- No CI yet (WS-15 in flight). Render/perf lanes unverified (no GPU box).

## Editor slots
- Held by the batch: agents serialise through `editor-lock.sh` (one editor process) and UBT's mutex. No human session should open the editor GUI until the batch completes.

---

# Cycle 2 (evening): the batch lands, the queue forms

## Landed on `unreal/main`
- **WS-15** harness: `DFTests`, `Testing/DFTestUtils.h`, `test.sh` with the JSON-report verdict, `smoke-listen.sh -client-count`, non-exec `editor-lock.sh`, GitHub workflows, `CONTRACTS/ci.md`.
- **WS-01** commandlet half: `Source/DFContentPipeline` (`-run=DFContentImport`), 20 `DT_*` tables, tests RoundTrip / TagCoverage / Bindings / SubsystemLoads. `StealthWeight` → `StealthWeightFactor` in `FDFConditionRow`, spelled as `conditions.json` (C2 append; `content-rows.md` updated this cycle).
- INT: `int-merge.sh --resume` + the ledger-only landing loop; `modules.json` gains DFContentPipeline (layer 6, Editor); RFC-0001 landed (sealing rule follows the sim harness gate 22c; itineraries carry node ids); `ownership-check.py` expands `{a,b}` globs; OWNERSHIP rows for legacy-imported levels, terrain outputs, `terrain.schema.json`, `Source/DFContentPipeline`, `L_<Map>.umap` (WS-09 until a WS-10x claims), `tools/waveplan-golden/**` (WS-05).

## In flight (order of landing)
1. **WS-11** — review passed; `int-merge` verify build queued on the UBT mutex (main moved with code since the first verification, so it rebuilds).
2. **WS-02** — fix round after review at WIP `b50f588`: `L_Test_Status` functional test, uninitialised `FLinearColor` palette defaults, refresh keeps the original DoT applier (Ember by applier, as `Step.cs`), hero status restriction, cue notify base, faction passive GEs; rules layer now matches `Step.cs` damage order and the fixed 30 Hz DoT tick.
3. **WS-09** — follow-up after review at `5d517d3` (commandlets make LFS-lockable packages writable; read-only save fix); building.
4. **WS-30** — fixes done; its ownership artefacts were INT-side and are resolved on main.
5. **PR #42 [WS-05]** — `FDFWavePlan` ported bit for bit from `WavePlan.cs`, RNG streams + `DFDetMath`, `tools/waveplan-golden` (.NET golden vectors), `FromContent` through `UDFContentSubsystem`, 11 tests incl. `DF.Unit.WavePlanBaseline`; `pr-check --filter DF.Unit.WavePlan --smoke` green on the author's side; INT read-only review started this cycle.

## Interface notes (contract-append / RFC texts sitting in ws files)
- `status.md`: statuses' GEs are C++ classes (WS-02). `gas.md`: damage order follows `Step.cs` (WS-02). `balance.json` dials `shredFrontArcLeakFactor` / `postArmorDamageFloor` / `rearThresholdDegrees` (WS-02, RFC text in `ws-02`). `DFTintLayout.h` CPD indices append — Needs INT. `DFTestUtils.h` and `ci.md` landed with WS-15.
- `docs/gate-baseline.tsv`: `foundry/4p` = **203 is a two-seated-player match; 356 is four** (WS-05). `DF.Unit.WavePlanBaseline` reads it that way.

## Machine notes
- `editor-lock.sh`'s `exec` skipped the EXIT trap and leaked a lock for 45 min; hotfixed on main with WS-15's version. Stale lock dirs removed by hand; the lock is clear tonight.
- UBT `-WaitMutex` is **not FIFO**. Three editor builds queued this evening (wt-ws09, int-merge-ws-11, wt-ws12); a full `DeepFieldEditor` build is 10–15 min on this Mac, so a 3-deep queue is ~40 min of wall time before the last one starts. Batch prompts should say so.
- **FP determinism**: UBT 5.8 with `BuildSettingsVersion.V7` compiles Editor targets FP-precise but leaves Game/Client/Server at Default (= `/fp:fast` on MSVC). Anything that must be identical across hosts (the wave plan) pins by pragma; unverified until the GPU box builds a Game target. Line added to `CONTRACTS/ci.md` for the GPU-box lane. Also worth checking there: clang's default `-ffp-contract=on` fuses multiply-adds, which `DFDetMath` must not rely on.
- The session host restarted mid-cycle; every task survived (the workflow retried the interrupted WS-02 agent, the bash landing loop and the WS-09 agent kept their processes). Nothing was relaunched.

## Blockers for the human (updated)
1. EOS Developer Portal product + sandboxes + client policies; EGS onboarding — unchanged; WS-11 stops at the Null services.
2. **GPU workstation: arrived, nothing installed.** A Windows bring-up runbook is coming as a `[WS-15] docs:` PR (`unreal/Build/windows-bringup.md`) from the WS-05 session; after it: UE 5.8.2, VS 2022, git-lfs full fetch, DDC, runner registration `[self-hosted, windows, gpu]`.
3. Register this Mac as the nightly self-hosted runner (`ci.md` says how) — still open.

## Editor slots
- Agents only, through `editor-lock.sh` and the UBT mutex. No human GUI session until the queue drains.

## Cycle 2, late: WS-11 landed, WS-30 landing, PR #42 reviewed
- **WS-11 landed** (`b992762..71f39fe` + STATUS `9a450a6`): C14 facade `UDFOnlineSubsystem` over `UE::Online` (listen backend, join codes, content hash), C13 `UDFProfileSave` v1 + migrations + local progression + match-record cap + match-save skeleton, `DF.Online` automation tests, `Config/Tags/DF_Online.ini`, additive `[OnlineServices]` / `[OnlineServices.Lobbies]` sections in `DefaultEngine.ini` (reviewed by INT on landing). **ADR-0022 accepted**: OSSv2 stays; no reason to fall back to OSSv1 was found on the 5.8.2 launcher build. Not proven until the portal product exists: EOS login, an EOSGS lobby, two machines over the relay — the credential-time checklist is in the ws-11 file; the EOS ini block is `rfcs/needs-int-eos-config.md`.
- **INT queue from WS-11's Needs INT / open questions** (an INT code batch after WS-02 and WS-09 land, so DFCore appends do not collide): `ADFGameMode::PreLogin` calls the join validator through a DFCore seam (`IDFJoinValidator`; DFMatch at layer 4 cannot see DFOnline); `IDFSanctionsCheck` / `IDFAntiCheatCheck` seams next to it, pass-through at L1; a requester online id on `DF.Message.JoinRequest` (`FDFMsg_Player` carries only seat + name — C15 append); WS-02's `DFTintLayout.h` append. Level curve placeholder (`1 + floor(Xp/xpPerLevel)`, cap 20) is WS-07's to replace. `UDFMatchSave` payload TODOs name WS-04/06/03/02/09/24.
- **`int-merge.sh`** now regenerates `STATUS.md` on a rebase conflict, records the verified base beside the worktree (a hand-rebased `--resume` is verified again, never trusted), lands STATUS as its own commit, and pulls the clone only when it is clean.
- **WS-30 landing launched** (fixes from its review are on the branch; the ownership artefacts were INT-side and were fixed on main).
- **PR #42 [WS-05] reviewed** (20 agents, 3 confirmed of 8): (1) major — `FName::ToString()` as the tie-break key in `PlanWave` and as the golden hash/sort key in the tests; on Game targets `WITH_CASE_PRESERVING_NAME` = 0 so the string is whatever spelling the engine registered first (`EKeys::Escape` makes every Spire plan hash differently) — key on the authored lowercase id; (2) major — `Out.Waves.SetNum(Row->WaveIndex + 1)` before the index is bounded (a bad row is a host crash or a multi-GB allocation) — bound to `[0, TotalWaves)`; (3) minor — a `0` co-op dial is refused as "missing" (0 is the sentinel) — NaN sentinel. Refuted: same-def tie order, `DFDetRng.h` outside the pragma, layering, half-to-even coverage (hand-authored table pins it), extreme-lap overflow. Held: golden vectors regenerate byte-identical from the real `Sim.Core`; RNG / `PowInt` / scales match statement by statement; baseline semantics correct. Fixes requested from the author; INT re-reviews the delta, then lands.
- **WS-30 landing — two binary collisions ruled by INT (§6.9 owner wins):** (1) `Content/DF/Maps/Foundry/L_Foundry.umap` — the persistent level — is added by both WS-30's `DFTerrainImport` and WS-09's importer; WS-09 owns `L_<Map>.umap` until WS-10a claims, so WS-30's copy is **dropped at landing** and the terrain lane lands `L_Foundry_Terrain.umap` only. Follow-ups: WS-09's `build_level.py` registers `L_<Map>_Terrain` as a streaming sublevel when it exists; WS-30's commandlet stops writing the persistent level. (2) `Content/DF/Core/PhysicalMaterials/PM_Grass.uasset` — physmats are WS-09's per §5.2 (new OWNERSHIP row); WS-09's branch has none, so this new file is accepted on landing and is WS-09's from here. The landing runs with `--ws INT` after this explicit review; every other file in the change set is under WS-30's globs.
