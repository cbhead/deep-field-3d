# INT digest — 2026-09-24 (cycle 3: the trunk catches up, and the ledger stops lying)

Written by session-62767025 (WS-05) acting INT. **The integration session that ran 19–21 September
has ended, and so has every agent it spawned.** Nobody inherited the role, so this digest covers
three days in which the trunk was idle, several branches were finished but unlanded, and `STATUS.md`
claimed nine active workstreams that nobody was working.

## Landed this cycle
- `[WS-05]` **`FDFLaneWalker`** — `Step.cs MoveEnemies` ported with ADR-0018's slope rules: position
  is (edge, segment, metres into it), a warp is crossed at any speed including zero, routing happens
  at a node and nowhere else, and nowhere-to-go is *stranded* rather than leaked. Siege routing
  prices walls so a Ram walks at a barricade instead of standing at it sorting last.
- `[WS-05]` **`KnockBack`** — displacement travels the lane, not space, so the lane graph stays the
  sole router. One edge of memory; nothing is pushed back through a warp.

## The stale-lease sweep
Every lease in the ledger had expired, the oldest by six days, mine included. Nine workstreams read
`active` or `review` with owners whose sessions no longer exist. Per §6.2 INT may reassign an expired
lease after logging it, so: **WS-00, 01, 02, 09, 11, 12, 15 and 30 are released to `paused`** with
their owners cleared. Nothing was deleted — every session log, open question and "Needs INT" note
stays in the workstream file, and a `paused` workstream with a full log is a workstream to *read*
before starting, not one to start over. WS-05 stays `active`; it has a live session.

This matters more than it sounds. A session reading `STATUS.md` on the 24th would have concluded
that eight workstreams were taken and there was nothing to claim, which was false in every case.

**`paused` here does not mean unfinished.** WS-02's C4/C5 and WS-09's lane graph are *on main* — those
workstreams read `active` because their agents died mid-round, not because the work stopped halfway.
The one genuine gap is **WS-00**, which sits at `review` only because its DoD says "CI builds" and CI
has never built anything (see below); that is WS-15's lane, not a defect in the foundation.

## What is stranded
- **PR #44 (`ws/12-ui/tokens-screens`)** — WS-12's UI tokens and CommonUI screen stack. Finished,
  mergeable, and its session ended before it landed. It is the largest piece of completed work not
  in the trunk and it should land before `DFUI` moves under it.

## Machines
**The GPU workstation arrived** (ADR-0023). ADR-0016's Mac-only constraint is over. The bring-up
runbook `unreal/Build/windows-bringup.md` landed on the 21st and has never been executed; the session
on that machine should work through it and fix it in place where reality differs.

## Red tests / unverified lanes
- **`unreal-mac` has still never run.** Zero self-hosted runners are registered, so the only lane
  that builds or tests the Unreal tree has never executed, and a green PR check means the ledger and
  schemas are consistent and nothing else. Every verification to date is a developer's local run.
  Registering the runner is the highest-value task in the programme right now.
- Render, perf, Windows-packaging and Gauntlet lanes remain unverified — the box exists but has not
  produced a result.
- 15 of 87 registered tests are in no filter any landing runs (`fff5d0c`): `DF.Online`, `DF.Editor`,
  `DF.UI`, `DF.Func`.

## For the human
1. **Register the Mac (or the GPU box) as a self-hosted runner.** Nothing else fixes "we have no CI".
2. **EOS Developer Portal** product, sandboxes and client policies; EGS onboarding. WS-11 stops at
   the Null services without them, and the invite-only session in G2 cannot be proved.
3. The Mac's memory pressure is now a correctness problem, not a speed one — see `CONTRACTS/ci.md`.
