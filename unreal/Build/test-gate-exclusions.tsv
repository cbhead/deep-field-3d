# Test prefixes the landing filter (int-merge.sh / ci-local.sh) does NOT run, each with an owner and a
# reason. Read by test-gate-check.py: a registered DF.* test that is neither gated nor listed here fails
# the check, and so does a row here that matches no test. CONTRACTS/ci.md (INT, 2026-09-21): run the
# full `DF` filter once for ground truth, then widen the landing filter to every green suite and keep a
# row here only for what stays out, with the reason it stays out.
#
# prefix	owner	reason
DF.Online.	WS-11	never run since authoring; awaiting INT's full-DF ground-truth run (ci.md 2026-09-21)
DF.Editor.	WS-30	never run since authoring; awaiting INT's full-DF ground-truth run (ci.md 2026-09-21)
DF.UI.	WS-12	never run since authoring; awaiting INT's full-DF ground-truth run (ci.md 2026-09-21)
DF.Func.	WS-02	WS-02's DoD functional test; never run since authoring; awaiting INT's full-DF ground-truth run (ci.md 2026-09-21)
